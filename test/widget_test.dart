// Placeholder — see capture_system_test.dart and move_evaluator_test.dart for
// the real gameplay-logic unit tests. A full app-boot widget test would need
// asset bundle access (CardRepository.load()) that isn't set up here yet.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
